    # def post(self,user_id,user_pw,user_name,user_gender,user_email,user_phone):
    #     # MySQL Server connect
    #     cur = conn.cursor()
        
    #     # user add
    #     # MySQL user table values add
    #     sql = "insert into user values(%s, %s, %s, %s, %s, %s, %s)"
    #     DT = datetime.datetime.today()
    #     user_date = DT.strftime('%Y/%m/%d %H:%M:%S')
        
    #     vals = (user_id, user_pw, user_name, user_gender, user_email, user_phone, user_date)
    #     cur.execute(sql,vals);
    #     conn.commit()

    # api.add_resource(user_join_membership, "/user_join_membership/<string:user_id>/<string:user_pw>/<string:user_name>/<string:user_gender>/<string:user_email>/<string:user_phone>")
    
    
    
    # @api.route('/kim',methods=['POST'])  # 데코레이터 이용, '/kim' 경로에 클래스 등록
# class Kim(Resource):
#     def post(self):  # GET 요청시 리턴 값에 해당 하는 dict를 JSON 형태로 반환
#         # body에서 요청받은 PersonName, PersonAge를 가져옴
#         name = request.json['PersonName']
#         age = request.json['PersonAge']
        
#         # MySQL 서버에 접속하기
#         cur = conn.cursor()
#         # 추가
#         sql = "insert into Persons values(%s, %s, %s)"
#         vals = (str(id),name,age)
#         cur.execute(sql,vals);
#         conn.commit()

#         return {"kim" : "junseok"}


; # 이미지 파일을 받는 메소드
; class file(Resource):
;     def post(self):
;         f = request.files['image']
;         f.save("C:\\vsCode\dream_dream_explan\save_image\\test.png")
; api.add_resource(file,"/fileUpload")